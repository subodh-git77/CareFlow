import { AlertCircle, CheckCircle2, Inbox } from 'lucide-react';

export const PageHeader = ({ eyebrow, title, children, action }) => <div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.2em] text-teal-600">{eyebrow}</p><h1 className="text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">{title}</h1>{children && <p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">{children}</p>}</div>{action}</div>;
export const Notice = ({ type='error', children }) => children ? <div className={`mb-6 flex items-start gap-3 rounded-2xl border p-4 text-sm ${type === 'success' ? 'border-emerald-200 bg-emerald-50 text-emerald-800' : 'border-rose-200 bg-rose-50 text-rose-800'}`}>{type === 'success' ? <CheckCircle2 size={18}/> : <AlertCircle size={18}/>}<span>{children}</span></div> : null;
export const Empty = ({ title='Nothing here yet', text='New items will appear here.' }) => <div className="card grid min-h-64 place-items-center text-center"><div><Inbox className="mx-auto mb-4 text-slate-300" size={42}/><h3 className="font-bold">{title}</h3><p className="mt-1 text-sm text-slate-500">{text}</p></div></div>;
export const StatusBadge = ({ status }) => { const color = status === 'CONFIRMED' ? 'bg-teal-50 text-teal-700' : status === 'COMPLETED' ? 'bg-indigo-50 text-indigo-700' : status === 'CANCELLED' ? 'bg-rose-50 text-rose-700' : 'bg-amber-50 text-amber-700'; return <span className={`rounded-full px-2.5 py-1 text-[11px] font-bold tracking-wide ${color}`}>{status}</span>; };
export const Loader = () => <div className="grid min-h-56 place-items-center"><div className="h-8 w-8 animate-spin rounded-full border-4 border-teal-500 border-t-transparent"/></div>;